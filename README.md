The way I get those up and running is by using chmod +x file.sh

Then you may wanna link them to /usr/local/bin using sudo ln -s /home/finn/file.sh /usr/local/bin/file

And that's it, feel free to copy, edit, re-publish, fork this repo. And code suggestions/good advice is well received :D

I use a bunch more shell scripts to open applications and load audio modules, but are not as easy to implement as the ones in this repo
